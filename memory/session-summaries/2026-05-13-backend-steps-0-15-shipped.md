# 2026-05-13 — Backend build, Steps 0–15 all shipped

**TL;DR** — One session, 22 commits, 117 tests green. AI Alpha Radar backend is functionally complete: fetchers → normalize → score → cluster → Claude enrichment → predictions → demand clusters → daily snapshot → Cloudflare Worker → GH Actions cron → frontend wired to data.json. Deploy + prompt-tuning are the only open items.

## What we discussed

- Built the whole BACKEND_BUILD.md spec end-to-end in spec order (Steps 0–15)
- Two credential exposures (PAT, Anthropic key) — both pasted into chat; recommended rotation
- One §5 one-card inspection on world-model-agents — Dean's verdict: "bad, fix prompts later, just get the infrastructure right"
- Multiple spec deviations surfaced not silent:
  - arXiv now HTTPS-only (301-redirects the spec's http://)
  - arXiv's `cat:X+OR+cat:Y` doesn't parse via httpx params dict — switched to ` OR ` so URL encoding hits the right operator
  - HN Algolia and GitHub Search both reject `topic:X OR topic:Y` for qualifiers — fan-out + dedupe pattern in both fetchers
  - arXiv 24h lookback misses the 17:00 UTC announce batch — bumped default to 2 days
  - `pipeline/normalize.py`: spec said drop <3 char tokens; bumped to <2 so "AI", "ML", "RL", "NLP" survive
  - `pipeline/score.py`: `lifecycle_stage(t: Trend)` and `tbts(t: Trend)` would be circular; switched to keyword-only args
  - `pipeline/predict.py`: extended `Prediction` with optional keyword/lifecycle_at_filing/target_lifecycle for deterministic verdicts (backwards-compat via Optional+None)
  - CI workflow pins Poetry 2.4.1 not 1.8.3 (spec) because the local lockfile was generated by 2.4.1
  - GitHub PAT was rotated mid-session; orchestrator now gracefully degrades fetcher failures into `meta.sources.{name}.ok=false`
- `gh auth refresh -s workflow` required so the workflow YAML could be pushed

## What we decided

- Repo lives at https://github.com/kuhnhomeuk-cell/ai-alpha-radar (public, owner `kuhnhomeuk-cell`)
- Niche default `"AI tools for solo creators"`; frontend in same repo under `public/`
- `.env.local` is the secrets file; `load_dotenv(..., override=True)` is the right pattern (parent shell can override with empty values)
- Infrastructure-first when prompts and plumbing both need work — saved as a feedback memory in the per-project memory dir
- Daily cron ships without `--claude` flag for now (placeholder enrichment); flip after prompt tuning
- One-card §5 inspection is the gating ritual before any Claude batch
- Per-step Conventional Commits with multi-line bodies, push after each step

## What shipped (Steps 0–15)

- `pipeline/` — 8 modules (models, fetch/arxiv, fetch/github, fetch/hackernews, fetch/semantic_scholar, normalize, score, cluster, summarize, predict, demand, snapshot, run)
- `worker/` — TypeScript Cloudflare Worker with /proxy/arxiv, /proxy/s2, /api/deep-dive, /api/spend (origin allowlist, KV-backed spend cap)
- `.github/workflows/daily-snapshot.yml` — 06:00 UTC cron + workflow_dispatch
- `public/data.json` (56KB, 30 trends), `public/snapshots/2026-05-13.json`, `public/index.html` (mockup-b wired to data.json)
- `scripts/gen_contract.py`, `scripts/backtest_predictions.py`, `scripts/inspect_one_card.py`
- 117 pytest tests; one live Anthropic call ($0.01) for the one-card inspection

## What's next (in your hands)

1. **Rotate keys** — the PAT (`github_pat_11CCCWFE…`) and the Anthropic key (`sk-ant-api03-LE_d2p-…`) were both pasted into chat. Revoke at GitHub settings and console.anthropic.com/settings/keys, regenerate, drop new values into `.env.local` via your terminal (not chat).
2. **Set repo secrets** at https://github.com/kuhnhomeuk-cell/ai-alpha-radar/settings/secrets/actions: `ANTHROPIC_API_KEY`, `GH_PAT`, optionally `SEMANTIC_SCHOLAR_KEY`.
3. **Enable GitHub Pages**: Settings → Pages → Deploy from a branch → `main` / `/public` → save.
4. **Deploy the Worker**: see `worker/README.md` — `wrangler login`, KV namespace create, secrets put, `npm run deploy`.
5. **Trigger the cron**: `gh workflow run daily-snapshot.yml` to validate it commits a fresh snapshot.
6. **Prompt tuning pass** — when you're ready, iterate on the four prompts in `pipeline/summarize.py` with one-card inspections (~$0.01 each) until the cards read right; then flip the workflow to `--claude`.
7. **README "How to add a new source"** — the last open §12 DoD item; cosmetic.

## Outstanding §12 DoD items

- [x] `poetry run python -m pipeline.run` runs locally without error (verified 2026-05-13)
- [x] `public/data.json` validates against `Snapshot` schema
- [x] ≥30 trends in the snapshot
- [ ] Trends have non-null summaries+angles — currently placeholders; waits on prompt tuning + `--claude`
- [ ] ≥4 demand clusters with real quotes — waits on `--claude` (mock-tested only)
- [x] ≥10 historical predictions in `past_predictions` (15 seeded; 13 in past_predictions after verdict update)
- [x] HitRate displays sensible value (`0.0` on day 1, expected)
- [ ] GitHub Actions cron has run successfully ≥1 time — needs secrets
- [ ] Cloudflare Worker `/proxy/arxiv` and `/api/deep-dive` return 200s — needs deploy
- [x] Frontend `mockup-b.html` renders against real `data.json` with no crashes
- [x] Daily Claude spend under $0.10 (one-card test was $0.01)
- [ ] README has "How to add a new source" section
